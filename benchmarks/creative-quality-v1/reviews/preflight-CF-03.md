# Preflight — CF-03

This is a production preflight, not a blind-review score.

## Gate checks

- **Dimensions:** pass — 1080 × 1350 PNG.
- **Claim accuracy:** pass — persistent state and its survival across restarts, deploys, and hibernation are stated in the current official quick start.
- **Code provenance:** pass — `initialState` and `setState` are derived from the current official `CounterAgent` example.
- **Generated-region boundary:** pass — generated pixels contain only transient fragments, a stable state substrate, and the environment.
- **Product fidelity:** pass — code, claims, labels, and product name are deterministic overlays.
- **Text legibility:** pass at full size; verify again at feed-preview size.
- **External publication:** not cleared. This is an internal benchmark study.

## Visual assessment before blinding

- The transient-versus-persistent contrast is visible before reading the body copy.
- The code card connects the metaphor to a real implementation surface.
- The deterministic annotation aligns with the persistent illuminated cell.
- The code card overlaps some of the lower substrate; the overlap is deliberate but should be tested at feed size.

## Eligible changes under the two-minute finishing test

- Move or resize the code card.
- Reposition the persistent-state callout.
- Reduce the body copy or darken the upper veil.

Changing the code or persistence statement requires another provenance review.
